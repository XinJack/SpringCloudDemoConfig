server:
  port: 8082
spring:
  application:
    name: order-server
  datasource:
    url: jdbc:mysql://localhost:3306/learning?characterEncoding=utf-8&useSSL=false
    username: root
    password: root
env:
  test
